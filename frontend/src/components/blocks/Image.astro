---
const { src, alt = '', caption } = Astro.props.data || {};
---

{src && (
  <figure class="block-image">
    <img src={src} alt={alt} loading="lazy" width="720" height="480" decoding="async" />
    {caption && <figcaption>{caption}</figcaption>}
  </figure>
)}
