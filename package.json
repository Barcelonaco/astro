{
  "name": "cms-astro",
  "version": "1.0.0",
  "description": "CMS headless multi-composant avec frontend statique Astro et backend PHP",
  "scripts": {
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "preview": "cd frontend && npm run preview"
  },
  "keywords": [
    "astro",
    "cms",
    "php",
    "mysql"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
